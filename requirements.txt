# Telegram监听机器人依赖
telethon    # Telegram API 客户端库
requests    # 用于Bark推送HTTP请求
pytz        # 时区处理
python-okx         # OKX官方API库

# 数据持久化依赖
sqlalchemy  # SQL数据库ORM
fastapi     # Web API框架
jinja2      # Jinja2 模板引擎（FastAPI 模板依赖）
uvicorn     # ASGI服务器（本地开发备用）
hypercorn   # ASGI服务器（支持 HTTP/2，用于 Northflank）
python-multipart  # 文件上传支持

# 可选依赖
# logging     # 日志记录（如果需要） 
python-dotenv  # 本地开发自动加载.env.local 
